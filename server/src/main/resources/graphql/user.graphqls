
extend type Query {

    getUserProfileInfo(userId: ID!): UserDto

}

extend type Mutation {

    updateUsername(userId: ID!, newUsername: String!): UserDto

    updateUserPassword(userId: ID!, oldPass: String!, newPass: String!): UserDto

}

type UserDto {
    userId: ID!
    email: String
    username: String
    roles: [String]
}